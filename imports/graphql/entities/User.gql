type User {
  _id: ID!
  emails: JSON
  name: String
  profile: Profile,
  posts: [Post]
}

input CreateUserProfileInput {
  name: String
  role: String
}

type Profile {
  name: String
  role: String
}
